wardrowbe-mcp {{ .Chart.AppVersion }} is installed as release "{{ .Release.Name }}".

In-cluster MCP endpoint:
  http://{{ include "wardrowbe-mcp.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}/mcp

Port-forward to try it locally:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "wardrowbe-mcp.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

Connect from Claude Code (replace <api-key> with the bearer you configured):
  claude mcp add --transport http wardrowbe \
    http://localhost:{{ .Values.service.port }}/mcp \
    --header "Authorization: Bearer <api-key>"
