apiVersion: v1
kind: Service
metadata:
  name: {{ include "wardrowbe-mcp.fullname" . }}
  labels:
    {{- include "wardrowbe-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "wardrowbe-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
