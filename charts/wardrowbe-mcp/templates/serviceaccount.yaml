{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "wardrowbe-mcp.serviceAccountName" . }}
  labels:
    {{- include "wardrowbe-mcp.labels" . | nindent 4 }}
{{- end }}
